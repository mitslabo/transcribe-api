[project]
name = "transcribe"
version = "0.1.0"
description = "FastAPI service for merging audio files and transcribing them with Whisper"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115.0",
    "httpx>=0.27.0",
    "uvicorn[standard]>=0.30.0",
]
